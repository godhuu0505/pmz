"""マルチエージェント構成（要件 §4.1 / §8.1 MVP must-have ⑥）.

オーケストレータ＋3つの読むエージェント（CodeRisk / PM-Req / Retrospective）で構成する。

**権限分離（§7 #3 ③ / インバリアント #1）が設計の核**:
読むエージェントは PR由来の（信頼できない）入力を取り込んで **構造化シグナル・所見・提案**
を出すだけで、**承認権限を持たない**。最終判定（``GateDecision``）を出せるのは Orchestrator が
呼ぶ ``judge()`` だけで、その根拠は構造化シグナルのみ。これにより「注入文だけで判定を覆す」
ことを構造的に不可能にする。
"""
